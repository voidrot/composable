{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the fragment"
    },
    "description": {
      "type": "string",
      "description": "A brief description of the fragment"
    },
    "variables": {
      "type": "object",
      "description": "Default environment variables required by the fragment",
      "additionalProperties": {
        "type": "string"
      }
    },
    "env_file": {
      "type": "boolean",
      "description": "Whether to inject .env.compose file configuration"
    },
    "env_file_vars": {
      "type": "array",
      "description": "Variables that should also be documented in .env",
      "items": {
        "type": "string"
      }
    },
    "configs": {
      "type": "array",
      "description": "Configuration files to copy alongside the fragment",
      "items": {
        "type": "object",
        "properties": {
          "source": { "type": "string" },
          "target": { "type": "string" }
        },
        "required": ["source", "target"]
      }
    },
    "build": {
      "type": "object",
      "description": "Build configuration to inject into the top-level compose service"
    },
    "watch": {
      "type": "array",
      "description": "Develop watch configuration to inject into the top-level compose service"
    }
  },
  "required": ["name"]
}
